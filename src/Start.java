import java.io.File;
import java.io.IOException;
import java.util.Scanner;

@SuppressWarnings("unused")
public class Start {
	//I have no clue what this is
	/*public static void main(String[] args) throws IOException{
		String ConfigFilePath = "Config.shde";
		File ConfigFile = new File(ConfigFilePath);
		Scanner ConfigFileScanner = new Scanner(ConfigFile);
		boolean HasLine = ConfigFileScanner.hasNextLine();
		
		String FilePath = null;
		for(;HasLine;){
			FilePath = ConfigFileScanner.nextLine() + ".shde";
			File File = new File(FilePath);
			if(!File.exists()){
				System.out.println("Hi");
				Setup.main(args);
			}
			if(!HasLine){
				break;
			}
		}
		ConfigFileScanner.close();
		//PasswordChecker.main(args);
		Worker.main(args);
	}*/
}